import { getStylesHook } from 'hooks/useStyles';

export default getStylesHook({
  chartValueContainer: {
    height: 32,
  },
  chartSkeleton: {
    height: 213,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
