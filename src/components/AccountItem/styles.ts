import { getStylesHook } from 'hooks/useStyles';

export default getStylesHook({
  container: {
    flexDirection: 'row',
  },
  image: {
    width: 40,
    height: 40,
    marginRight: 10,
    borderRadius: 20,
  },
});
