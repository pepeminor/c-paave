import {} from 'styles';
import { getStylesHook } from 'hooks/useStyles';

export default getStylesHook({
  disableButton: {
    opacity: 0.5,
  },
  leftContainer: {
    height: 24,
    width: 297,
  },
});
