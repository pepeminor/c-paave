import { FollowingType } from 'screens/CopyTradeScreen/CopyTradeScreen.type';

export type CopyTradeBtnProps = {
  followingType: FollowingType;
  followingUsername?: string;
  followingFullName?: string;
};
