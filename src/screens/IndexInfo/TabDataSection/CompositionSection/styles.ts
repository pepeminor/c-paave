import { getStylesHook } from 'hooks/useStyles';

export default getStylesHook({
  selectedTab: {
    paddingVertical: 9,
  },
  tableContainer: {
    height: 810,
  },
  containerHotStock: {
    flex: 1,
    minHeight: 240,
  },
});
